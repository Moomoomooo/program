import { Entity } from '#data/Entity.ts';
import { SkillTargets } from '#enums/SkillTargets.ts';

export const Niam: Entity = new Entity("Niam", [10, 0, 0, 1, 5], "d10");

Niam.addSkill("power attack", 3, )
Niam.addSkill("fire bolt", 2, )
